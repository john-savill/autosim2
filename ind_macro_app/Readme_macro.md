# INFO

Independent macro-runner

Runs the same Macro operations that are in the app but in a headless mode, i.e:

```
./macro_runner my_macro.csv
./macro_runner my_macro.csv --verbose
./macro_runner my_macro.csv --dry-run
./macro_runner --help
```

( The dry-run will show what would be executed without actually doing it)
